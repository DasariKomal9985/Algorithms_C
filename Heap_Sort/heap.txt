Uses a Binary Heap (Max-Heap) to repeatedly extract the maximum element.

How it works (logic)

Build a max heap from the array.

Swap the first element (largest) with last.

Reduce heap size and heapify.

Repeat until array sorted.

Complexity

Time: O(n log n)

Space: O(1)

Use when: Need guaranteed O(n log n) without extra memory.