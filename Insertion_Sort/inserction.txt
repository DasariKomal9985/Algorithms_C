Builds the sorted array one element at a time by “inserting” each element into its correct place.

How it works (logic)

Start from index 1 → key.

Compare key with left side elements.

Shift bigger elements to the right.

Insert key in correct position.

Complexity

Time: O(n²)

Space: O(1)

Use when: Array is small or nearly sorted (best case O(n)).